<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{ staticBasePath }}/error.bundle.css">
</head>
<body>
    {{> header/index _title=title _user=user }}
    <h3>Произошла ошибка</h3>
    <p>{{error.message}}</p>
    {{#if error.errors}}
        <ul>
        {{#each error.errors}}
            <li>{{this}}</li>
        {{/each}}
        </ul>
    {{/if}}
    <script src="{{ staticBasePath }}/error.bundle.js" async="async"></script>
</body>
</html>
